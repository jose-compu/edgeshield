# EdgeShield — documentation branch

This branch hosts the [GitHub Pages](https://jose-compu.github.io/edgeshield/) site.

- Edit files under `docs/`
- Push to `docs` to publish
- Application source lives on `main`

Local preview from `main`:

```bash
npm run docs:serve
```

GitHub Pages settings: **Branch `docs` · Folder `/docs`**
